create-pool meta
create-pool data

! exec restic-rados-server --pool=$CEPH_POOL_DATA:data,snapshots --pool=$CEPH_POOL_META:index --listen $WORK/restic.sock
stderr 'blob types not assigned to any pool'
stderr 'config'
stderr 'keys'
stderr 'locks'
